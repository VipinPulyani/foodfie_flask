import numpy as np
import pandas as pd
import pymysql

from foodfie import config


def connectDB():
    conn = pymysql.connect(host = config.host, port=config.port, user= config.user, password = config.password, db= config.db)
    cur = conn.cursor()
    return conn, cur
